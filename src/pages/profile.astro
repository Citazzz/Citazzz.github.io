---
import BaseLayout from '../layouts/BaseLayout.astro';
// 1. 引入 Mail 图标
import { Github, Terminal, Cpu, Layers, ExternalLink, MapPin, User, Mail, Zap, Coffee } from 'lucide-react';

const bio = {
  name: "CITAZZZ",
  role: "Developer / Student",
  id: "PKU_CS_2025",
  location: "Beijing, CN",
  // 2. 在这里填写你的邮箱
  email: "citazzz@outlook.com", 
  intro: `北京大学元培学院 2025 级本科生，主修计算机科学与技术。`
};

// 定义默认数据变量
let githubProfile = {
  username: "Citazzz",
  url: "https://github.com/Citazzz",
  public_repos: 0,     
  followers: 0,        
  bio: "Loading..."    
};

// 构建时获取真实数据
try {
  const res = await fetch('https://api.github.com/users/Citazzz');
  if (res.ok) {
    const data = await res.json();
    githubProfile = {
      username: data.login,
      url: data.html_url,
      public_repos: data.public_repos, 
      followers: data.followers,       
      bio: data.bio || "No bio available"
    };
  }
} catch (error) {
  console.error("GitHub API fetch failed:", error);
}

interface Project {
  name: string;
  desc: string;
  tech: string[];
  status: string;
  link: string;
}

const projects: Project[] = [
    // 这里暂时为空，等待你添加项目
];

const getStatusColor = (status: string) => {
  if (status === 'Dev') return 'text-yellow-500 border-yellow-500 bg-yellow-500/10';
  if (status === 'Finished') return 'text-green-500 border-green-500 bg-green-500/10';
  return 'text-blue-500 border-blue-500 bg-blue-500/10';
};
---

<BaseLayout title="My Profile">
  <div class="max-w-6xl mx-auto space-y-8 animate-in fade-in duration-700">
    
    <div class="flex items-end justify-between border-b border-gray-200 pb-4">
      <div>
        <h1 class="text-4xl font-black text-rhine-dark">PERSONAL_DATA</h1>
        <p class="text-xs font-mono text-gray-400 mt-1">PERSONNEL_FILE // {bio.id}</p>
      </div>
      <div class="hidden md:block text-right">
        <!-- 
            🟢 彩蛋修改处：
            1. div 改为 a 标签
            2. 加上 href 跳转链接
            3. 加上 hover 变色效果，提示用户可点击
        -->
        <a 
            href="https://space.bilibili.com/399918500?spm_id_from=333.337.0.0" 
            target="_blank" 
            class="text-[10px] font-mono bg-rhine-green text-white px-2 py-1 cursor-pointer hover:bg-emerald-600 transition-colors select-none"
            title="点击前往 Miku 的空间 -w-"
        >
            Love Miku -w-
        </a>
      </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-12 gap-8">
      
      <!-- 左侧栏 -->
      <div class="lg:col-span-4 space-y-6">
        <div class="bg-white border border-gray-200 p-1 shadow-sm group relative overflow-hidden">
           <div class="absolute top-0 left-0 w-full h-1 bg-rhine-green z-10"></div>
           
           <div class="p-6 relative z-0">
              <div class="flex justify-between items-start mb-6">
                <div class="w-20 h-20 bg-gray-100 border border-gray-300 flex items-center justify-center text-gray-400 overflow-hidden">
                    <!-- 确保 public 文件夹下有 profilePic.png -->
                    <img src="/profilePic.png" alt="Citazzz Avatar" class="w-full h-full object-cover hover:scale-110 transition-transform duration-500" />
                </div>
                <div class="text-right">
                    <div class="text-2xl font-black text-rhine-dark">{bio.name}</div>
                    <div class="text-xs font-mono text-rhine-green">{bio.role}</div>
                </div>
              </div>

              <div class="space-y-3 font-mono text-sm text-gray-600">
                <!-- 地理位置 -->
                <div class="flex items-center gap-3">
                    <MapPin size={14} />
                    <span>{bio.location}</span>
                </div>

                <!-- 3. 联系方式 -->
                <a href={`mailto:${bio.email}`} class="flex items-center gap-3 hover:text-rhine-green transition-colors group/email cursor-pointer">
                    <Mail size={14} className="group-hover/email:text-rhine-green transition-colors" />
                    <span>{bio.email}</span>
                </a>

                <div class="w-full h-px bg-gray-100 my-2"></div>
                
                <div class="whitespace-pre-line leading-relaxed text-xs text-gray-500 font-sans">
                    {bio.intro}
                </div>
              </div>
           </div>

           <div class="bg-gray-50 p-3 flex justify-between items-center text-[10px] font-mono text-gray-400 border-t border-gray-100">
              <span>STATUS: ONLINE</span>
              <div class="flex gap-1">
                 <span class="w-2 h-2 bg-rhine-green rounded-full animate-pulse"></span>
              </div>
           </div>
        </div>

        <!-- Github 卡片 -->
        <a href={githubProfile.url} target="_blank" class="block bg-rhine-dark text-gray-300 p-6 font-mono text-xs relative overflow-hidden group border border-rhine-dark hover:border-rhine-green transition-colors">
            <div class="absolute top-3 right-3 text-rhine-green opacity-20 group-hover:opacity-100 transition-opacity">
                <Github size={24} />
            </div>
            <div class="mb-4 flex gap-2 text-gray-500">
                <div class="w-3 h-3 rounded-full bg-red-500"></div>
                <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
                <div class="w-3 h-3 rounded-full bg-green-500"></div>
            </div>
            <div class="space-y-2">
                <div class="flex gap-2">
                    <span class="text-rhine-green">➜</span>
                    <span class="text-blue-400">~</span>
                    <span>whoami</span>
                </div>
                <div class="pl-4 text-white font-bold">{githubProfile.username}</div>
                
                <div class="flex gap-2 pt-2">
                    <span class="text-rhine-green">➜</span>
                    <span class="text-blue-400">~</span>
                    <span>git status</span>
                </div>
                <div class="pl-4 text-gray-400">
                    On branch main<br>
                    Your branch is up to date with 'origin/main'.<br>
                    <br>
                    <span class="text-rhine-green">Public Repos: {githubProfile.public_repos}</span><br>
                    <span class="text-rhine-green">Followers: {githubProfile.followers}</span>
                </div>
                
                <div class="flex gap-2 pt-2 group-hover:animate-pulse">
                    <span class="text-rhine-green">➜</span>
                    <span class="w-2 h-4 bg-rhine-green"></span>
                </div>
            </div>
        </a>

        <!-- Sponsor 能量模块 -->
        <div class="mt-4">
            <details class="group relative">
                <!-- 1. 交互按钮 (Summary) -->
                <summary class="list-none [&::-webkit-details-marker]:hidden cursor-pointer">
                    <div class="bg-rhine-surface border border-gray-300 p-4 flex items-center justify-between hover:border-rhine-green hover:shadow-[0_0_15px_rgba(140,198,63,0.2)] transition-all duration-300 group-open:bg-rhine-dark group-open:border-rhine-dark group-open:text-white">
                        
                        <div class="flex items-center gap-3">
                            <div class="w-8 h-8 bg-gray-100 rounded-sm flex items-center justify-center text-gray-400 group-hover:bg-rhine-green group-hover:text-black group-open:bg-rhine-green group-open:text-black transition-colors">
                                <Zap size={16} className="fill-current" />
                            </div>
                            <div class="flex flex-col">
                                <span class="text-xs font-black tracking-wider group-open:text-rhine-green">ENERGY_SUPPLY</span>
                                <span class="text-[10px] font-mono text-gray-400 group-hover:text-rhine-green/70">STATUS: AWAITING</span>
                            </div>
                        </div>

                        <!-- 右侧指示器：加号变减号 -->
                        <div class="font-mono text-xs text-gray-400 group-hover:text-rhine-green group-open:text-rhine-green">
                            <span class="block group-open:hidden">[+]</span>
                            <span class="hidden group-open:block">[-]</span>
                        </div>
                    </div>
                </summary>

                <!-- 2. 展开后的内容 (Content) -->
                <div class="bg-rhine-dark border-x border-b border-gray-800 p-4 animate-in slide-in-from-top-2 duration-300">
                    <p class="text-[10px] font-mono text-gray-400 mb-4 leading-relaxed">
                        // SYSTEM_LOG:<br>
                        感谢对本终端和Citazzz贡献的开源项目的支持。您的赞助将用于维持服务器运行。
                    </p>
                    
                    <div class="grid grid-cols-2 gap-3">
                        <!-- 选项 A: 链接跳转 (默认显示亮灰色，悬停变绿) -->
                        <a href="https://www.buymeacoffee.com/citazzz" target="_blank" class="flex flex-col items-center justify-center gap-2 p-3 border border-gray-600 bg-gray-900 text-gray-300 hover:border-rhine-green hover:text-rhine-green hover:bg-rhine-green/10 transition-all cursor-pointer">
                            <Coffee size={20} />
                            <span class="text-[10px] font-bold">BUY COFFEE</span>
                        </a>

                        <!-- 选项 B: 微信/支付宝 (默认显示亮灰色，悬停变绿并弹出二维码) -->
                        <div class="relative group/qr flex flex-col items-center justify-center gap-2 p-3 border border-gray-600 bg-gray-900 text-gray-300 hover:border-rhine-green hover:text-rhine-green hover:bg-rhine-green/10 transition-all cursor-pointer">
                            <!-- 这里用人民币符号作为图标 -->
                            <span class="text-xl font-bold leading-none">¥</span>
                            <span class="text-[10px] font-bold">WECHAT</span>

                            <!-- 悬停时弹出的二维码浮层 -->
                            <div class="absolute bottom-full left-1/2 -translate-x-1/2 mb-3 w-36 p-1 bg-white border-2 border-rhine-green opacity-0 invisible group-hover/qr:opacity-100 group-hover/qr:visible transition-all duration-300 z-50 shadow-[0_0_20px_rgba(140,198,63,0.3)]">
                                <!-- 二维码图片 -->
                                <img src="/qr.png" alt="QR Code" class="w-full h-auto block" />
                                <div class="text-[8px] text-center text-black bg-rhine-green font-bold py-1 mt-1">SCAN TO PAY</div>
                                
                                <!-- 下方的小三角箭头 -->
                                <div class="absolute top-full left-1/2 -translate-x-1/2 w-0 h-0 border-l-[6px] border-l-transparent border-r-[6px] border-r-transparent border-t-[6px] border-t-rhine-green"></div>
                            </div>
                        </div>
                    </div>
                </div>
            </details>
        </div>
      </div>

      <!-- 右侧栏 -->
      <div class="lg:col-span-8">
         <div class="flex items-center gap-4 mb-6">
            <Layers size={20} className="text-rhine-green" />
            <h2 class="text-xl font-bold text-rhine-dark">CURRENT_PROJECTS</h2>
            <div class="h-px bg-gray-200 flex-1"></div>
         </div>

         <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {projects.map(project => (
                <a href={project.link} class="group bg-white p-6 border border-gray-200 hover:border-rhine-green transition-all duration-300 flex flex-col justify-between h-full hover:-translate-y-1">
                    <div>
                        <div class="flex justify-between items-start mb-4">
                            <div class={`text-[10px] px-2 py-1 font-mono border ${getStatusColor(project.status)}`}>
                                {project.status.toUpperCase()}
                            </div>
                            <ExternalLink size={16} className="text-gray-300 group-hover:text-rhine-green transition-colors" />
                        </div>
                        
                        <h3 class="text-lg font-bold text-rhine-dark mb-2 flex items-center gap-2">
                            {project.name}
                        </h3>
                        <p class="text-sm text-gray-500 leading-relaxed mb-6">
                            {project.desc}
                        </p>
                    </div>

                    <div class="flex flex-wrap gap-2 mt-auto">
                        {project.tech.map((t) => (
                            <span class="text-[10px] font-mono text-gray-400 bg-gray-50 px-2 py-1 group-hover:bg-rhine-bg group-hover:text-rhine-dark transition-colors">
                                #{t}
                            </span>
                        ))}
                    </div>
                </a>
            ))}

            <div class="border border-dashed border-gray-300 p-6 flex flex-col items-center justify-center text-gray-400 gap-2 min-h-[200px]">
                <Cpu size={32} className="opacity-20" />
                <span class="text-xs font-mono">AWAITING_NEW_MISSION...</span>
            </div>
         </div>
      </div>

    </div>
  </div>
</BaseLayout>